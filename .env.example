# Local environment variables (do NOT commit real secrets)
# Copy this to .env and fill your real API keys locally, then `source .env` or use your preferred env tool.

BRAVE_API_KEY=changeme
# Optionally set AGGO_LLM_MODEL or other env vars here for local testing
# AGGO_LLM_MODEL=deepseek-r1:1.5b
